name: Set up node
description: Set up Node.js

runs:
  using: composite

  steps:
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        cache: npm
        node-version-file: .nvmrc
